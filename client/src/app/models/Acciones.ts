/*import { Title } from '@angular/platform-browser';

export interface Perfil {
    Carnet?: number;
    Nombres?: string;
    Apellidos?: string;
    contrasena?: string;
    correo?: string;
    
};*/
import { Title } from '@angular/platform-browser';

export interface Acciones {
    idAccion?: number;
    nombre?: string;
    descripcion?: string;
    bastones?: string;
    edad?: number;

    
};