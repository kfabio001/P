/*import { Title } from '@angular/platform-browser';

export interface Perfil {
    Carnet?: number;
    Nombres?: string;
    Apellidos?: string;
    contrasena?: string;
    correo?: string;
    
};*/
import { Title } from '@angular/platform-browser';

export interface listaAcciones {
    idLista_A?: number;
    idHijo?: number;
    
    idAccion?: number;
    estado?: string;
   

    
};