/*import { Title } from '@angular/platform-browser';

export interface Perfil {
    Carnet?: number;
    Nombres?: string;
    Apellidos?: string;
    contrasena?: string;
    correo?: string;
    
};*/
import { Title } from '@angular/platform-browser';

export interface Admin {
    idAdmin?: number;
    Nombre?: string;
    email?: string;
    contra?: string;
};